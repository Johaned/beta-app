= form_for @appraissal do |f|
  - if @appraissal.errors.any?
    #error_explanation
      %h2= "#{pluralize(@appraissal.errors.count, "error")} prohibited this appraissal from being saved:"
      %ul
        - @appraissal.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name
  .field
    = f.label :description
    = f.text_area :description
  .actions
    = f.submit 'Save'
